const User = require('../controllers/User/User');

module.exports = (app) => {

    // POST
    app.post('/', (request, response) => {
        //
    })

    // GET
    app.get('/', (request, response) => {
        //
    })
}
